#!groovy
// Check ub1 properties
// Define variable
def myVariable = "$server"

// Print variable
pipeline {
  agent any
  stages {
    stage ("Print variable") {
      steps {
        echo "My variable is ${myVariable}"
      }
    }
  }
}
