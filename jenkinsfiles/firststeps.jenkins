#!groovy
// Check ub1 properties
// Define variable
def myVariable = "127.0.0.1"

// Print variable
pipeline {
  agent any
  stages {
    stage ("Print variable") {
      steps {
        echo "My variable is ${myVariable}"
      }
    }
  }
}
